using UnityEngine;

public class CollidableObject : MonoBehaviour
{
    // The purpose of this component is to mark GameObjects of interest in the scene
    // and distinguish them via strong types.
}